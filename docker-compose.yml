version: '3.8'
services:
  astralis-api:
    build: .
    container_name: astralis-api
    ports:
      - "8000:8000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
